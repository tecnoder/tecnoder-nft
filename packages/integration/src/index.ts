import { myCommons } from "@sindric-lib-ui/commons";
import { myComponent } from "@sindric-lib-ui/components";
import { createDropUrl } from "@sindric-lib-ui/endpoints";

console.log(myCommons());
console.log(myComponent());
console.log(createDropUrl);